
export async function post(req) {
    try {
        const body = req.formData
console.log(body)
     // const response = await pocketBase.post(path, formData);
     // return response.data;
    } catch (error) {
     // throw new Error(`Error posting data to ${path}: ${error.message}`);
    }
  }